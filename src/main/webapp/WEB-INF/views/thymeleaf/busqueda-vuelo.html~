<!DOCTYPE HTML>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Búsqueda de Vuelo</title>

  <!-- Bootstrap core CSS -->
  <link rel="stylesheet" th:href="@{/webjars/bootstrap/5.3.5/css/bootstrap.min.css}"/>

  <!-- Custom CSS -->
  <link rel="stylesheet" th:href="@{/css/main.css}" />
</head>

<body>
<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">UNLAM</a>
  </div>
</nav>

<main class="container" style="margin-top: 80px;">
  <h1 class="mb-4">Buscar vuelo</h1>

  <form th:action="@{/busqueda-vuelo}" method="post" class="row g-3">
    <div class="col-md-6">
      <label for="origen" class="form-label">Origen</label>
      <input type="text" class="form-control" id="origen" name="origen" required>
    </div>
    <div class="col-md-6">
      <label for="destino" class="form-label">Destino</label>
      <input type="text" class="form-control" id="destino" name="destino" required>
    </div>
    <div class="col-md-6">
      <label for="fechaIda" class="form-label">Fecha de ida</label>
      <input type="date" class="form-control" id="fechaIda" name="fechaIda" required>
    </div>
    <div class="col-md-6">
      <label for="fechaVuelta" class="form-label">Fecha de vuelta</label>
      <input type="date" class="form-control" id="fechaVuelta" name="fechaVuelta" required>
    </div>
    <div class="col-12">
      <button type="submit" class="btn btn-primary">Buscar vuelo</button>
    </div>
  </form>

  <div th:if="${vueloUrl}" class="mt-4">
    <h2>Tu mejor vuelo</h2>
    <ul class="list-group">
      <li class="list-group-item"><strong>Origen:</strong> <span th:text="${vuelo.origen}"></span></li>
      <li class="list-group-item"><strong>Destino:</strong> <span th:text="${vuelo.destino}"></span></li>
      <li class="list-group-item"><strong>Fecha ida:</strong> <span th:text="${vuelo.fechaIdaStr}"></span></li>
      <li class="list-group-item"><strong>Fecha vuelta:</strong> <span th:text="${vuelo.fechaVueltaStr}"></span></li>
      <li class="list-group-item"><strong>Valor ida:</strong> <span th:text="${valorIda}"></span></li>
      <li class="list-group-item"><strong>Valor vuelta:</strong> <span th:text="${valorVuelta}"></span></li>
    </ul>
  </div>

  <div th:if="${error}" class="alert alert-warning" role="alert">
    <span th:text="${error}"></span>
  </div>
</main>

<!-- Bootstrap JS -->
<script type="text/javascript" th:src="@{webjars/bootstrap/5.3.5/js/bootstrap.min.js}"></script>
</body>
</html>
